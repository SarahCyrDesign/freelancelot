<h1>Freelancers</h1>
<% @users.each do |user| %>
  <h3><%= user.email %></h3>
  <h3>Current projects: <%= user.projects.length %></h3>
  <p><%= link_to 'View', user_projects_path(user) %></p>
  <% end %>
