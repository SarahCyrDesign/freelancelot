<% @categories.each do |category| %>
  <ul>
    <li><%= link_to category.name, category_path(category) %></li>
  </ul>
<% end %>

<% if current_user.admin? || current_user.freelancer? %>
  <%= link_to "Add New Category", new_category_path %>
<% end %>
